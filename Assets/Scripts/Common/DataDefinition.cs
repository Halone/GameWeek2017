public enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT
}

public enum ArmyType {
    PLAYER1,
    PLAYER2
}

public enum CenterContent {
    VOID
}

public enum TileContent {
    VOID,
    KNIGHT,
    WIZARD,
    BOWMAN
}